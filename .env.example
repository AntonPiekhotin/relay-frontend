# Everything client-facing goes through the Vite proxy on the dev origin, so these are PATHS,
# not hosts. Pointing them at http://localhost:8080 breaks CORS and the WS Origin check.
# See docs/ARCHITECTURE.md §2.
VITE_API_BASE=/api/v1
VITE_WS_PATH=/ws
